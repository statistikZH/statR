% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/insert_worksheet_nh.R
\name{insert_worksheet_nh}
\alias{insert_worksheet_nh}
\title{insert_worksheet_nh}
\usage{
insert_worksheet_nh(
  data,
  workbook,
  sheetname = "data",
  title = "Title",
  source = "Quelle: Statistisches Amt Kanton Zürich",
  metadata = NA,
  grouplines = FALSE
)
}
\arguments{
\item{data}{data to be included in the XLSX-table.}

\item{workbook}{workbook object to write new worksheet in.}

\item{sheetname}{name of the sheet-tab.}

\item{title}{title of the table and the sheet}

\item{source}{source of the data. Defaults to "statzh".}

\item{metadata}{metadata-information to be included. Defaults to NA.}

\item{grouplines}{defaults to FALSE. Can be used to separate grouped variables visually.}

\item{logo}{path of the file to be included as logo (png / jpeg / svg). Defaults to "statzh"}

\item{contactdetails}{contactdetails of the data publisher. Defaults to "statzh".}
}
\description{
Function to create formatted spreadsheets automatically
}
\examples{
 # insertion of two worksheets in a existing workbook

  # create workbook
wb <- openxlsx::createWorkbook("hello")

insert_worksheet_nh(mtcars[c(1:10),],wb,"mtcars",c(1:4),carb,grouplines=c(1,5,6))

  # create workbook
export <- openxlsx::createWorkbook("export")

# insert a new worksheet
insert_worksheet_nh(head(mtcars)
                  ,export
                  ,"data1"
                 ,title = "Title"
                  ,source = "Quelle: Statistisches Amt Kanton ZÃ¼rich"
                  ,metadata = "Bemerkung: ...")

 # insert a further worksheet
insert_worksheet_nh(tail(mtcars)
                   ,export
                   ,"data2"
                   ,title = "Title"
                   ,source = "Quelle: Statistisches Amt Kanton ZÃ¼rich"
                   ,metadata = "Bemerkung: ...")

 # save workbook
 openxlsx::saveWorkbook(export,"example.xlsx")

}
\keyword{insert_worksheet}
